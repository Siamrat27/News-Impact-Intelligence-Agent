# Phase 1-2: ingest + schema
feedparser>=6.0
psycopg[binary]>=3.1
python-dotenv>=1.0

# Phase 5-6: embeddings (SPEC §6.2 resolved) — local ONNX, free, no API key
fastembed>=0.5

# Phase 7-9: agent graph. LLM provider is env-driven and free-tier friendly
# (Groq by default); langchain adapters for whichever provider is configured.
langgraph>=0.2
langchain-groq>=0.2
langchain-anthropic>=0.2

# Phase 10: API
fastapi>=0.110
uvicorn[standard]>=0.29
psycopg-pool>=3.2
